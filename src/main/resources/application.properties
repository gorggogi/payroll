spring.application.name=payroll
app.base-url=${APP_BASE_URL:http://localhost:8080}
# zeroDateTimeBehavior: legacy DB rows may use 0000-00-00; driver otherwise throws "Zero date value prohibited"
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/u738281641_payroll?zeroDateTimeBehavior=CONVERT_TO_NULL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:}

# Connection pool (recommended for Spring Boot)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.connection-timeout=30000


spring.jpa.hibernate.ddl-auto=none


spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

spring.mail.host = smtp.mailjet.com
spring.mail.port = 587

spring.mail.username = ${MAILJET_USERNAME:08012c4292293155fed4da51da4b35ee}
spring.mail.password = ${MAILJET_PASSWORD:09a9d80ff2ffae3d854587e4672c560a}

spring.mail.properties.mail.smtp.auth = true
spring.mail.properties.mail.smtp.starttls.enable = true

# Holiday sync

spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB